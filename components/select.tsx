
export function Select(props: React.ComponentProps<"select">) {
  return (
    <select {...props} className="block bg-zinc-900 text-zinc-300 px-3 py-2 w-full rounded outline-none">
    </select>
  )
}
